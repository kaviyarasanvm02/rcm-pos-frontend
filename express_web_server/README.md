# Steps to server the /build content via Express
1. Run `npm i` to install Express and other dependencies
2. Copy the build/ contents generated by `npm run build` to the ./build folder
3. Start the server
	`node server.js`